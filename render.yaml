services:
  - type: web
    name: rasa-flask-app
    env: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: PORT
        value: "8000"
      - key: RASA_ENV
        value: "production"
